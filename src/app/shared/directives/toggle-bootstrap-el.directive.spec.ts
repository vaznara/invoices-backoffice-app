import { ToggleBootstrapElDirective } from './toggle-bootstrap-el.directive';

describe('ToggleBootstrapElDirective', () => {
  it('should create an instance', () => {
    const directive = new ToggleBootstrapElDirective();
    expect(directive).toBeTruthy();
  });
});
