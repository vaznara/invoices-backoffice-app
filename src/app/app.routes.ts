import { Routes } from '@angular/router';
import { BootstrapTestComponent } from './pages/bootstrap-test/bootstrap-test.component';

export const routes: Routes = [
  { path: '', component: BootstrapTestComponent },
];
